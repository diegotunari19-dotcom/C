#include <stdio.h>
int main()
{ 
int numeros[3]; 
printf("INSIRA 3 NUMEROS:\n"); 
scanf("%d", &numeros[0]); 
scanf("%d", &numeros[1]); 
scanf("%d", &numeros[2]); 
if (numeros [0]>= 0){ 
	printf("%d = POSITIVO\n" , numeros[0]);} 
else { 
	printf("%d = NEGATIVO\n" , numeros[0]);} 
if (numeros [1]>= 0){ 
	printf("%d = POSITIVO\n" , numeros[1]);} 
else{ 
printf("%d = NEGATIVO\n" , numeros[1]);} 
if (numeros [2]>= 0){ 
	printf("%d = POSITIVO\n" , numeros[2]);} 
else {
	printf("%d = NEGATIVO\n" , numeros[2]);}
return 0;
}
