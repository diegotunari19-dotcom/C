#include <stdio.h>

int mdc(int x , int y);

int main(){
		int a, b;
		printf("INFORME DOIS VALORES: ");
		scanf("%d %d", &a , &b);
		printf("O MDC DOS VALORES: %d", mdc( a, b ));
		return 0;
}

int mdc(int x , int y){
	int i;
	if (x>y){
		 i = x;
	}
	else{
		i = y;
	}
	for (;x % i != 0 || y % i != 0; i-- ){
			}

	return i;
}
		
		
